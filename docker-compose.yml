version: '3.8'

services:
  web:
    image: nginx:alpine
    container_name: vsp_kalpataru_web
    ports:
      - "3001:80"
    volumes:
      - ./web:/usr/share/nginx/html:ro
    networks:
      - vsp_kalpataru_net

networks:
  vsp_kalpataru_net:
    driver: bridge
